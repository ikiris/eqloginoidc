---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: eqloginoidc
spec:
  entryPoints:
    - web
  routes:
    - match: (PathPrefix(`/auth`) || PathPrefix(`/token`) || PathPrefix(`/userinfo`) || PathPrefix(`/login`))
      kind: Rule
      services:
        - name: eqloginoidc
          port: 8443
