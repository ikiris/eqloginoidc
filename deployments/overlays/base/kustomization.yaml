apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: eqloginoidc

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

images:
  - name: ghcr.io/ikiris/eqloginoidc
    newTag: 0.1.7
